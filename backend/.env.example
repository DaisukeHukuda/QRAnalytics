# データベース接続情報
DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DATABASE

# JWTシークレット（任意の英数字）
JWT_SECRET=your_jwt_secret

# Redis接続情報（Redis未使用なら空欄でもOK）
REDIS_URL=redis://USER:PASSWORD@HOST:PORT

# サーバーポート（通常は3000）
PORT=3000
